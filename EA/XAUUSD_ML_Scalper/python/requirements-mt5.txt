# Optional, Windows-only: needed only for download_from_mt5.py.
# Install this on the machine where your MT5 terminal actually runs.
# It will NOT install on Linux/macOS - use ExportHistoryCSV.mq5 there instead.
MetaTrader5>=5.0.45
pandas>=1.5
